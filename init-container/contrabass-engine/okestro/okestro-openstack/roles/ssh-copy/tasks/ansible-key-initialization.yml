- name: SSH key initialize
  community.crypto.openssh_keypair:
    path: /root/.ssh/id_rsa

- name: Reset user-password file & SSH env configuration
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "/root/.ssh/config"
    - "/root/.ssh/ansible-user-password.txt"

- name: Create config files
  file:
    path: "{{ item }}"
    state: touch
    owner: root
    group: root
    mode: 0600
  with_items:
    - "/root/.ssh/config"
    - "/root/.ssh/ansible-user-password.txt"

- name: Set password to server password on deploy container
  user:
    name: root
    password: "{{ server_password | password_hash('sha512') }}"

- name: Enable ssh service on deploy container
  service:
    name: sshd
    state: restarted

- name: SSH env configuration
  lineinfile:
    path: "{{ item.path }}"
    line: "{{ item.line }}"
  with_items:
    - { path: "/root/.ssh/config", line: "StrictHostKeyChecking no" }
    - { path: "/root/.ssh/ansible-user-password.txt", line: "{{ server_password }}" }

- name: SSH key copy
  shell: "sshpass -f '/root/.ssh/ansible-user-password.txt' ssh-copy-id {{ hostvars[item].ansible_host }}"
  with_items:
    - "{{ groups['alls'] }}"
